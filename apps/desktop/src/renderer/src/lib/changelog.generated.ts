// GENERATED by apps/desktop/scripts/gen-changelog.mjs — DO NOT EDIT.
//
// Source: CHANGELOG.md at the repo root. Edit that, then run
// `npm run gen:changelog` (or any build, which runs it via prebuild:main).
// changelog.test.ts fails when this file and the markdown disagree.

/** One "### Added" / "### Fixed" block under a release. */
export interface ChangelogSection {
  title: string;
  /** Entries with their inline markdown intact, one paragraph each. */
  items: string[];
}

export interface ChangelogRelease {
  /** "1.2.3", or "Unreleased" for the in-progress section. */
  version: string;
  /** ISO date from the heading; empty for Unreleased. */
  date: string;
  unreleased: boolean;
  sections: ChangelogSection[];
}

/** Newest first, exactly as CHANGELOG.md orders them. */
export const CHANGELOG: readonly ChangelogRelease[] = [
  {
    version: '0.162.0',
    date: '2026-09-02',
    unreleased: false,
    sections: [
      {
        title: 'Added',
        items: [
          "**A switch for the boot-time account-usage poll.** The agent daemon reads every signed-in Claude account's 5-hour and 7-day limits when it starts, so the usage cards are right before anything is running. That is still what happens by default, and `usage.pollOnBoot` is now the way to say otherwise: turn it off and the daemon polls only accounts that have a live session, so an idle machine makes no account-usage requests at all. The setting reaches the daemon in its environment at spawn from both places that start one, the desktop app and `workspacer serve`, and there is a checkbox for it in Settings under Session, beside keep-warm. A daemon started without the setting polls on boot, so nothing changes for an install that never touches it.",
          "**The Overview usage cards read the daemon directly when no session is running.** They drew their windows only from a live session's status line, so a freshly opened app showed nothing at all until the first agent started, no matter how good the daemon's own reading was. They now fall back to claudemon's usage report, which answers from disk and from the account poll with nothing running, refreshed every five minutes. A live status line still wins wherever there is one, per window rather than per card. A window the report says has already rolled over, or that carries no reset time, is shown as not running rather than as an empty meter: the percentage against a lapsed window is history, and drawing it would state a false present. The web and remote clients keep their previous behaviour, since the daemon is reachable only from the machine it runs on.",
          "**Effort stepping: a routing mode can now trim thinking time instead of changing the model.** Until now a mode had one lever — move the role to a different capability — which swaps the model out entirely. Between \"the same model\" and \"a different model\" there is a gentler move, and `mode_shifts.<mode>.effort_step` is it: a notch count on the provider's own reasoning ladder, applied to the row the answer lands on. Conserve ships at `-1`, spend_down at `+1`. It is a count rather than a level name because the ladders are not portable — Claude runs low..max, Codex stops at xhigh — so one notch down means the same thing on both. Where it fires is the point. A provider merely running *ahead* of its window curve — past `block_spend_down_at_ratio`, not yet at `conserve_at_ratio` — used to block a spend-down and do nothing else. It now also trims one notch and leaves the capability exactly where it is: being slightly ahead of the curve is a reason to spend a little less, not a reason to change which model does the work. The harsher tier shift stays where it was, for the band that needs it. That pace-armed trim is per-provider by its own rationale: when a fallover lands the answer on a different provider, the band is re-read for the provider that will run the work, and the trim applies only if that provider is itself running ahead. A conserve or spend_down step is not re-derived, because the mode owns the whole decision. Under conserve both levers can fire on one answer: a `judge` on `codex_only` drops from `frontier_plus` to `deep_reviewer` and from `high` to `medium` at once. Three rules bound it. A row that declares no `effort:` is not stepped — there is no rung to count from, and the answer says so rather than inventing a level nobody wrote. `effort_step_capabilities` limits the step to the tiers where reasoning is the expensive part (frontier, frontier_max, deep_reviewer, frontier_plus by default), because a scout at `medium` is mostly a worse scout rather than a cheaper one. And a per-row `min_effort` floor — set to `high` on every review capability in the shipped `mixed` and `anthropic_only` profiles, primaries and alternatives alike — is what stops a conserving fleet from trimming its own reviewer. A floor written ABOVE the row's own `effort:` is a contradiction the loader reports, and it disables stepping for that row rather than raising the effort: a floor holds an effort down and never lifts one. Under spend_down there is one promotion per decision, and the shipped reading of that rule is worth stating plainly. The router judges it on capability ranks rather than on whether a shift fired, because the ceiling runs between the two and routinely takes the promotion straight back: a spend_down implementer is moved from `frontier` to `frontier_max` and the shipped `default` ceiling clamps it back to `frontier` in the same decision. Nothing was promoted there, so nothing is held back, and the effort steps one rung ABOVE what the landing row declares. On the shipped `mixed` profile that is codex `gpt-5.6-sol` at `xhigh` where the `frontier` row says `high`: the same model, thinking longer, which is the gentler half of the promotion the ceiling refused. When the tier promotion does survive, the effort is left at the tier's own declared value instead. The bounds above are what keep that interpretation contained: `effort_step_capabilities` decides which tiers may step at all, so nothing outside the four frontier and review tiers is raised this way; a `min_effort` floor holds a downward step up; a `min_effort` written above the row's own `effort:` disables stepping for that row in either direction; and the provider's ladder is a hard stop, so a codex row already at `xhigh` stays there. The answer carries `effortStep` (from, to, why) whenever a step was armed, including when it was armed and then clamped or floored, and the field rides the `routing.decision` event and the decision log. `effort_step: 0` reproduces the previous answers byte for byte, reasons included.",
          '**Live provider availability is a fallover trigger.** Every reason the router could pass over a candidate was a fact about the routing file — a row switched off, a load-time issue, a health reading. One live fact was missing: a provider whose CLI runs and reports that it can launch no model at all, however healthy its allowance looks. The hub\'s existing per-provider model-catalog probe now also answers that question, and the map is injected into the decision rather than fetched inside it, so the policy layer stays pure. A provider whose CLI ran and listed no launchable model is routed around, with the reason naming it. Everything else is unknown and routes exactly as it did before, which is what keeps a hub that cannot reach its daemon for thirty seconds from declaring every provider dead. Three limits are worth stating plainly, because the obvious reading of the paragraph above is wider than the feature. It **cannot** tell you a CLI is not installed: a missing binary makes claudemon\'s `list_models` spawn fail, claudemon answers 502, and a failed probe reads as unknown rather than as unavailable. Unknown fails open, so an uninstalled provider is still offered work, and the spawn that fails there is the loud and immediate answer. It can never mark `claude` unavailable, because that catalog is assembled from alias names and ids seen in transcripts rather than reported by a running CLI, so an empty answer there means "I do not know". And it is consulted only inside the fallover walk, so a pinned provider, a capability with no `alternatives:`, and the provider a mode shift lands on are not checked against it. A verdict that found models is cached for the catalog TTL. An `unavailable` verdict and an unreachable daemon are both re-probed on the next background refresh, because a provider somebody has just installed or logged into is exactly the one whose verdict has gone stale.',
        ],
      },
      {
        title: 'Changed',
        items: [
          "`routing.select`'s answer, the `routing.decision` event, the decision log, the MCP `routing` help topic and the `select_model` tool description all describe effort stepping and availability. `docs/limit-aware-routing.md` gains a section per knob, including the honest limit: an effort step trims thinking tokens, not context re-reads, so it degrades gracefully and will not rescue a red window — the tier shift is still the tool that bends the curve.",
        ],
      },
      {
        title: 'Fixed',
        items: [
          '**The Claude usage bar no longer blanks for the first few minutes after you start an agent.** The card stayed, the meter emptied, and it filled itself back in some minutes later. Two causes, one symptom. In the daemon, an account rate-limit reading was treated as authoritative for five minutes but refreshed at worst every fifteen. A headless Claude session has no percentage of its own, because its wire event carries the reset time and leaves the utilization out, so the percentage on its status line comes entirely from that account reading, injected on each rebuild. Every rebuild landing in the ten-minute hole between "declared stale" and "refreshed" went out with no percentage, which is the empty bar. The freshness ceiling is now derived from the refresh cadence rather than written down beside it, so a reading is never retracted between two of its own polls, and a test ties the two constants together. The ceiling is still a ceiling: a reading from a poller that has died stops being asserted after one refresh cycle plus a minute of grace. The poller also booked its next attempt from whether the account was busy at the moment it last ran, so an account polled while idle kept a quarter-hour appointment even after you started work on it. A root that goes from idle to live is now due on the next scheduler tick, which is what makes the meter correct within half a minute of starting an agent rather than up to fifteen minutes later. Every poll outcome is logged, success and failure alike, with the account it was for and when it was observed, so a reading\'s age is something you can now account for instead of guess at. On the desktop, the Overview usage card remembered the last reading as one whole thing, so a newer status line carrying a reset time and no percentage displaced a good percentage, and a live reading older than what was remembered was drawn in preference to it. The card now remembers each figure separately: a line that does not mention a percentage says nothing about it, and the fresher of the two readings wins per field.',
        ],
      },
    ],
  },
  {
    version: '0.161.0',
    date: '2026-09-01',
    unreleased: false,
    sections: [
      {
        title: 'Added',
        items: [
          "**Limit-aware agent routing.** The hub now reads how much of each provider's subscription allowance is actually left and resolves the *kind* of work being dispatched into a concrete model and reasoning effort, so work moves off a provider whose window is tight instead of draining it. Whatever dispatches names a role (`scout`, `implementer`, `reviewer`, `judge` and the rest); a matrix resolves the role to a capability and a profile resolves the capability to a `(provider, model, effort)`. Nothing outside that file names a model, so a vendor rename is one edit. An operator-tier agent gets it as the `select_model` tool, which is read-only: it answers and starts nothing. The matrix is `routing.yaml` in the hub's own state directory, beside `jobs.json`. It is written there on first run with its comments intact, deep merged over the compiled-in defaults so anything you omit still resolves, and re-read on the same 30-second tick jobs use, so a saved edit applies with nothing restarted. A file that does not parse leaves the running matrix exactly as it was. Three profiles ship: `mixed` (the default, and the recommendation whenever both subscriptions exist, because it draws two allowances down in parallel and gives review a different model family from the implementer), `codex_only` and `anthropic_only`. The rule the whole thing rests on: **a usage reading is believed only while its reset time is still in the future.** A window that has already reset reads as *unknown* rather than as a stale percentage, because one expired row is wrong twice over. It makes a provider with a fresh allowance look two-thirds spent, and its reset look imminent when it already happened. `routing.yaml` also carries per-directory **ceilings**: the most capability and the most authority a spawn started in a given tree may be given, matched on the resolved path by longest ancestor. Every `agents.spawn` over the bus is clamped to it, and the clamp is reported back in the spawn's own answer rather than applied silently. The shipped default caps capability at `frontier` and leaves authority at `operator`, which is what a `judge` dispatch meets first: the role asks for `frontier_plus`, so out of the box it resolves to `frontier` and says so, and raising the ceiling is the way to get the reserved tier. Every decision and every clamped spawn is appended to `routing-decisions.jsonl` beside the matrix, joined by a decision id. Documented on the docs site and in `docs/limit-aware-routing.md`; there is no UI for the matrix, which is what keeps a ceiling out of reach of the agents it governs.",
          "**Routing reads how fast an allowance is being spent, not only how much of it is left.** Health answers what share of a window is gone. It cannot answer whether that share is a lot for the hour it is: the same 40% is comfortable six days into a seven-day window, and is a fleet about to run dry six hours into one. Claudemon now stamps the length of each Anthropic window onto its usage report: five hours and seven days, the two lengths the window names already assert and the endpoint never stated. The monthly overage window carries no length, because a calendar month is not a fixed number of minutes. The hub divides the share used by the share the window's curve expects by now, and the result is a ratio where 1.0 is exactly on the curve. `thresholds.pacing` in `routing.yaml` holds the two bands, the ratio at which the mode becomes conserve and the ratio at which being near a reset with capacity left stops licensing a spend-down, plus the bootstrap floor underneath them, since a window's first minutes divide by nearly nothing and every reading taken there would read as overspend. The seven-day window can be paced on a `calendar` curve, which ships and is linear in wall-clock time, or on a `workdays` curve that budgets a weekend hour at a fraction of a weekday hour in the host's own timezone, so a week's allowance spent across five working days is on plan rather than 40% over it. Every knob is validated at load. Pace can only tighten. It is read after the health verdict and before the spend-down, so it can add conserve and it can block a spend-down, and it can never talk a red or exhausted allowance back down or promote anything. A window nobody measured, and a reading the daemon flagged stale, both pace as *unknown* rather than confidently against data that was never taken. Where a provider has two readable windows, the worse ratio binds. `thresholds.pacing.enabled: false` reproduces the pre-pacing answers exactly, down to the absent fields on the decision and on the `routing.decision` event. Three smaller corrections ride with it: the workday curve walks local midnights and used to stall in a timezone whose DST transition lands at midnight, silently degrading to the calendar curve for any window spanning it; a stamped window length outside a sane range is refused rather than overflowed; and a legitimate on-curve ratio of exactly `0` now survives onto the wire instead of being dropped as an empty field.",
          "**A capability can name a second provider, and the router falls over to it.** Each capability in a routing profile may now carry an ordered `alternatives:` list, other providers that serve the same tier of work. When the primary pairing cannot be used, the router walks that list, takes the first candidate that can be, and names what it moved off in the decision's reasons and in a new `fellOverFrom` field. What makes a candidate unusable is judged with that candidate's own capacity, never the primary's: its allowance is red or exhausted, its own provider's mode verdict is conserve, the provider or the entry carries `enabled: false`, or the loader flagged that row. If nothing in the list can be used, the primary stands, exactly as it did before the key existed. The shipped `mixed` profile now names both a `claude` and a `codex` entry on every tier, held by a test, so no tier of work becomes unreachable because one subscription is down. Family independence for review changes shape with it. It is a preference the router honours by trying a different family first and then reports on, not a rule it enforces by refusing. A codex outage can therefore land a `mixed` reviewer back on codex, and the answer says so. `fresh: true` on the review capabilities is untouched and is still the only hard rule: a reviewer does not inherit the implementer's conversation, before or after a fallover. Health and pace on the `routing.decision` event now describe the provider that was chosen, so a reviewer that fell over from a red claude to a green codex is no longer published as `provider: codex, health: red`. Only the primary's `fresh:` is read, which makes an alternative's own setting advisory, so the loader raises an issue at load time when the two disagree rather than letting a fallover quietly carry the primary's flag. An existing `routing.yaml` keeps working unchanged, and gains the shipped alternatives through the same deep merge as any other key it omits. Its comments are the part that does not update: a file written before this release still says the `mixed` profile buys family diversity *by construction*, which is now stale prose beside correct behaviour. The shipped defaults, `docs/limit-aware-routing.md`, the `routing` help topic and the docs site all say the accurate thing.",
        ],
      },
      {
        title: 'Changed',
        items: [
          "**Context-window controls are now provider-scoped, and requests stay provisional.** Claude context variants may be selectable in supported live session controls, but Codex context-window choice is made at spawn time; existing Codex sessions show their runtime/provider-managed effective context read-only. The Fleet Manager keeps a separate saved preference for each provider. A fresh Codex session with no explicit choice requests a 1,000,000-token window, while a resume keeps its durable request when it has one and a legacy resume without one stays on Codex's provider/default behaviour. A requested window is not a promise that the runtime granted it: occupancy, context bars and `contextUsedPct` watches use only the provider's runtime-confirmed effective window.",
          '**Context health now reports only active, runtime-confirmed occupancy.** Legacy Codex telemetry that has no last-request usage now shows context waiting instead of inventing a percentage; Copilot occupancy uses prompt/input tokens rather than adding completion output; and `contextUsedPct` watches wait for runtime-confirmed active occupancy before they arm or fire.',
          "**Keep-warm knows Codex's window without a Codex session.** Warming a 5-hour window starts by asking whether one is already running, and for Codex that question used to be put to whatever live sessions happened to be open — so on a cold start, with no Codex agent running, the answer was \"no idea\" and keep-warm sent a warm ping that a still-open window did not need. It now reads the window from `GET /usage/report`, which reads the rollout the Codex CLI already wrote to disk, so a running window is recognised with nothing open. A live session's status line is still consulted, as the fallback for the one reading the rollout cannot settle. Codex's *percentage* is deliberately not used to decide this: a reading of 67% against a reset time that has passed is real history and a false present, and treating it as a live window would stop keep-warm warming at all. Claude's check is unchanged — `GET /usage` already answers with no session, for the one login that is warmed, and refetches on demand. This also gives `/usage/report` its first client: the endpoint shipped in 0.160.0 with nothing in the app reading it.",
          'The Windows containment corpus no longer asks NTFS to keep two spellings of one directory apart. Two cases built their fixture tree from two casings of a single name, which is one directory on a case-insensitive filesystem, so the denial they asserted was a false refusal, and the Windows CI leg failed around thirty subtests. Those cases now carry a `caseSensitiveOnly` flag and are skipped there, and the regression gains the Windows-native vector it was missing: a sibling whose path is the granted root plus more letters. No production code changed. The containment guard was right.',
        ],
      },
    ],
  },
  {
    version: '0.160.0',
    date: '2026-08-29',
    unreleased: false,
    sections: [
      {
        title: 'Added',
        items: [
          "**GitHub Copilot is a supported harness.** `copilot` joins Claude Code, Codex, OpenCode and Pi as a first-class managed backend, everywhere the others are: the spawn dialog, handoff, the Fleet Manager's dispatch, the MCP facade and `/m`. It runs one `copilot -p … --output-format json` process per turn and resumes by session id, so a restart genuinely preserves the conversation and a model or effort change lands on the next turn instead of needing a respawn. Two things behave differently from the other harnesses and the UI now says so rather than implying otherwise: Copilot cannot prompt for approval in non-interactive mode, so its permission pill reads **Workspace only** / **Full access** — the allow flags control path confinement, not a gate that will ask you; and its model catalogue cannot be enumerated and is account-gated, so the picker offers `auto` alone instead of ids the account would refuse. Copilot bills AI credits with no dollar figure on the wire, so cost is estimated from the shared pricing table, which gained the OpenAI rates the desktop side had been missing entirely.",
          "**Copilot can run the Fleet Manager.** `agents.managerProvider` now accepts `copilot` beside `claude` and `codex`. One caveat is surfaced in Settings when you select it, because it is the one manager harness whose tool access is not decidable before the session starts: a GitHub org policy can disable third-party MCP servers, and a manager that cannot reach Workspacer's tools cannot dispatch. That case raises a session error naming the missing servers rather than passing for a healthy manager.",
          "**Usage is there at boot.** The Overview cost tile, the Inspector's Usage tab and the History pane used to be able to answer only for a session that was running right now, so a restored agent showed nothing beside a database that had already recorded every dollar it spent. Those surfaces now fall back to the desktop's own recorded session history, so cost and token figures are real on a cold start with no session open. An absence stays a dash rather than a fabricated `$0.00`, and the dash says which absence it is: nothing was ever recorded for that session, or the recorded usage could not be read. Account rate-limit windows are the part that still needs a live session. After a restart the rate-limit cards render nothing at all, which also puts the new usage detail modal out of reach on the dashboard, though it still opens from the inspector and shows each window's duration alongside its percentage. The daemon can answer for windows without a live session, over `GET /usage/report`, which reads what each CLI already left on disk for Claude, Codex and Copilot. Nothing in this release reads that endpoint.",
          '**Account profiles for every harness, not just Claude.** A profile is a config root, and each harness has one: `CLAUDE_CONFIG_DIR`, `CODEX_HOME`, `COPILOT_HOME`. So a second Codex or Copilot login is now a profile you create in Settings, pick in the spawn dialog, and see attributed in usage. Codex additionally gets `-p <preset>`, kept deliberately separate from the root because a preset layers settings over the *same* account. Copilot is the one harness where the root is not the identity — its login lives in the OS credential store — so a Copilot profile names an environment variable holding a token, and the value is resolved at spawn and never written to `claude-profiles.json`. The Settings form, the spawn picker and rate-limit failover all follow the selected harness now, and a field a harness does not support is absent or disabled with the reason, instead of present and inert.',
          '**Remote worker nodes, and a Connect button.** A machine can now be off on purpose. The hub keeps a node registry with a four-state model — running, waking, stopped, unreachable — plus `nodes.list`, `nodes.wake` and a Fly Machines client to do the waking; a woken node is claudemon plus a headless brain, so its sessions arrive as ordinary sessions. The client half is a strip at the top of the sidebar on the desktop and `/app`, and at the top of the Fleet screen on `/m`, with the same surface in `wks-tui`. Waking costs money, so the button says so in plain words and asks for confirmation before it spends any; a wake that failed is shown as a machine still running and still billing, not as a silent nothing. A view- or triage-tier client sees the state and not the button, with the reason beside it. A hub with no node registry renders nothing at all, so nothing changes for an install that has none. `deploy/fly/` ships the node image, `fly.toml`, entrypoint and an ordered bring-up runbook.',
          "**Dispatch templates.** A new `dispatch` library kind holds a reusable prompt with named placeholders and an optional default result schema. `spawn_agent` takes `template` + `templateParams` and the host renders it at spawn, so a manager spends no tokens retyping boilerplate. A template is pure text with no trust boundary — tool scope, cwd, model, worktree and permissions always come from the caller and pass the caller's clamps — and an unfilled placeholder refuses the spawn by name rather than silently defaulting, so the task-specific reasoning still has to be written.",
          '**The first message rides the spawn.** Every spawn surface takes a `message`, delivered inside the spawn handler before it answers. A dispatch used to be spawn-then-send with a live worker holding no instructions in the gap, and on a headless session that send could land before the wrapper existed and be dropped with only a console error.',
          '**A headless Fleet Manager gets its wakes.** A manager running on a box with no desktop is now woken on a worker finishing, on catching up after it was away, and on a worker becoming blocked on an approval or a question — that last one broadcast to every live wake target rather than to a parent, since two live managers after a handoff is the ordinary case.',
          '**`fleet.quiescence`, and a power-down-when-quiet job.** A read-only bus method that answers whether this machine\'s fleet is genuinely at rest, so "shut the box down while nothing is happening" can be automated without reimplementing the safety logic in a shell script. It refuses when unsure: a session in an unrecognised state, a row that will not parse, a provider that does not answer or a peer it cannot reach all block, and `blockers[]` names each reason. Settings ▸ Jobs ships a template for it that arrives **disabled**, with a deliberately blank command, because the command is yours to write. `jobs.json` is also hand-editable now and re-read on the tick.',
          '**Plan, subagents and workflows on every harness that has them.** Copilot and OpenCode really do plan and delegate and the evidence was being dropped; both now fill the Plan and Agents tabs, including on `/app`. OpenCode subagents are routed to their own row instead of appearing as the parent\'s own work. On Pi — whose tool set has no plan or delegate verb — the tabs say so, instead of an indefinite "No plan yet".',
          '**Pickers only offer harnesses you have installed.** A spawn, handoff or manager picker no longer lists all five backends on a machine that has one. Detection failing open is the rule: a harness we have no answer for stays visible, and a harness named by your config or by a live session stays listed and flagged "not installed" rather than vanishing.',
          "**`/app` in a browser caught up with the desktop.** Real file attachments, plugin panes reachable from a browser on another machine, an iframe fallback for every `<webview>` pane, the transcript fetch a headless session's snapshot never carried, and `open_terminal`. Transcripts now arrive as deltas over the bus, subscribed per open pane, instead of re-downloading the whole in-progress reply every 500ms.",
          "**Brief and dispatch housekeeping.** A worker's structured result can be appended to the brief directly; `## Now` lines that outlived the dispatch that created them are flagged; over-long lines are refused with the section's size reported; and the archive move is a first-class operation rather than a hand edit.",
          '**Draft this with an agent.** Settings fields that want prose can be handed to an agent to draft, and the notification it raises knows which field it points at.',
        ],
      },
      {
        title: 'Changed',
        items: [
          '**BREAKING for mixed-version fleets: the enriched-snapshot key `isSupervisor` is now `isWakeTarget`.** The flag that marks a session as a wake target is renamed on the wire, in `agents.list`, and in every client, and there is **no dual-emit** — the old key is not written at all. A federation peer, a Fly node, or an installed `/m` PWA still running an older build will silently lose the MANAGER chip and crew nesting until it updates. **Update federated peers and any headless hub together with the desktop, and hard-refresh an installed `/m` PWA.** The rename is deliberate and lands before the key is frozen for public integrators: it is a wake target, which is what the code has meant by it since the supervisor role was retired.',
          '**Codex runs headless by default, on every platform including Windows.** `codex.transport` is a real config key shipping `stream`, the twin of `claude.transport`, with its own Settings row so the TUI+viewer hybrid stays one click away. Codex previously had no configured transport at all and four spawn entry points each guessed, which is why the same fleet came up GUI-only on one machine and hybrid on another. Both shapes now ride the wire explicitly. On Windows, a headless session that cannot bring its app-server up degrades to the hybrid in place — with a notice in the conversation and the terminal actually appearing — instead of hard-failing into a dead pane.',
          '**An unknown context window is no longer spelled `200,000`.** Four places asserted a 200k window on a session whose model was not even known yet, which is the "every session starts at 200k and upgrades later" complaint at its source. The limit is nullable end to end — absent on the wire means not reported, never zero — and the twelve render paths that already guarded on a positive limit now hide the meter for real. OpenCode and Pi sessions, and the first turn of a stream session, lose their context meters until something reports one. One shared contract table now settles the window for Rust, TypeScript and Go, closing a divergence where `gpt-5-codex` reported 272,000 through the status line and 200,000 through usage at the same instant for the same session.',
          'Workers see what they are **holding** rather than what they have billed, and a fleet card reads occupancy from the one place that guards it.',
        ],
      },
      {
        title: 'Fixed',
        items: [
          "**A config write could empty your `projects` map and report success.** The Go writer replaced the whole map with `{}` when a wholesale config path (`projects`, `ui.customThemes`, `claude.budgets`) received anything that was not a JSON object — a string, a number, `null` — taking every project's label, colour, icon, favourite, delivery mode and worktree hooks with it. Since every agent's config write goes through that path, this was reachable, not theoretical. A non-object at a wholesale path is now refused instead of coerced.",
          '**Sessions no longer report idle while work is live**, on three separate paths, and a dead subagent no longer holds its parent active while a live one reads idle.',
          '**Managed-provider stalls are diagnosed honestly** instead of every one of them being called "No signal".',
          '**Codex, OpenCode and Pi are no longer handed a Claude model id** on spawn, and a Codex turn that failed no longer gets swallowed.',
          '`agents.sendMessage` honours `fromSessionId` on the desktop, so a message from one agent to another is attributed to the sender.',
          '`spawn_agent` defaults its model to the configured default when the caller omits one, rather than to nothing.',
          'Restored sessions bill real cost: `transcript_path` is persisted, the account is recorded at spawn rather than inferred from a path, and attribution survives path canonicalisation.',
          'The price cache writes by TTL and the prompt-cache read/write split is surfaced; two live producers of absurd context numbers were stopped; and the model a session was *asked* for is recorded and persisted on every spawn path.',
          '**tmux-mode pane navigation uses the actual pane geometry**, not flat index arithmetic, so up/down go where you are looking.',
          '`mcp_ask` no longer takes over a pending approval slot it does not own.',
        ],
      },
      {
        title: 'Removed',
        items: [
          '**The supervisor role.** The `supervisor: true` spawn flag, the `/supervise` skill, and the `supervisor` config block are gone, along with the Settings ▸ Supervisor section (folded into Settings ▸ Fleet Manager). The Fleet Manager is now the only fleet-coordination role. "Ask the fleet" survives: it now spawns a plain agent at the `triage` tool tier instead of a supervisor.',
        ],
      },
      {
        title: 'Security',
        items: [
          '**The MCP facade no longer serves an uncredentialed caller.** It listens on `127.0.0.1:7897` over plain HTTP and exposes the whole fleet — `spawn_agent`, `send_message`, `write_file`, `save_config` — and it used to hand that entire operator tool set to any request that presented no token at all. Loopback is reachable by every process and every user account on the machine, and by a container sharing the host network namespace, so "reached the port" was effectively no authentication. The shipped default of the `-untokened` dial is now `deny`: `/mcp` and `/sse` answer 401 to a credential-less request, while `/health` stays open for liveness probes. **Nothing you spawn needs configuring.** Every facade session the desktop or the headless brain starts already mints its own per-session scoped token and presents it (an `Authorization` header on the generated `--mcp-config` for Claude PTY/stream, `?t=` on the URL for codex/opencode/copilot), so tiers and tools are unchanged for agents, workers and the Fleet Manager. The one thing this breaks is a *hand-configured* MCP client that sent no token: give it one with `workspacer token create --scope operator`, or set `facade.untokenedAccess: operator` in `config.yaml` to opt the whole facade back open. The `facade.untokenedAccess` key (`deny` | `view` | `operator`) is now documented — on the [docs](landing/docs.html) configuration page, in the [build & internals](landing/build.html) MCP section, in `services/hub/README.md`, and in `docs/remote-sharing-security.md` — including what each value costs. Unchanged, and worth stating because they were already right: the facade\'s DNS-rebinding `Host` guard, and the policy that refuses at startup to bind a non-loopback address without a credential. Also unchanged, and still a gap: claudemon\'s session API on `127.0.0.1:7891` accepts message injection and spawns from any local process with no credential.',
        ],
      },
    ],
  },
  {
    version: '0.150.0',
    date: '2026-08-23',
    unreleased: false,
    sections: [
      {
        title: 'Added',
        items: [
          '**The Fleet Manager.** A dedicated orchestrator agent — a real session at operator tier, not a script — that dispatches worker agents, watches them finish or block, and keeps a per-project brief. It runs on Claude or Codex (`agents.managerProvider`) and dispatches to all four supported harnesses. The manager never polls: it is woken on a worker finishing, blocking, hitting a threshold it armed, or reporting its own progress (`agents.reportProgress`), all through one wake channel.',
          '**The brief.** A living per-project markdown file the manager writes as work lands, with a shape that encodes decay — a `## Now` list that empties as work finishes, a durable `## Direction`, and a `## Recently` log that records not just what happened but *why*, including retracted decisions and do-not-re-dispatch markers. `/checkpoint` prunes and archives it rather than letting it grow forever; the new Board pane renders it as a kanban with drag-to-archive. `brief_append` is safe for a worker and the manager to write to at the same moment.',
          "**Fleet succession, while the app is running.** If a manager crashes or is closed, its workers are discoverable as orphans (`list_orphans`) and can be adopted by a successor (`adopt_workers`, `reparentChildren`); crashed managers leave a tombstone their successor can read. `agents.list` now reports `parentSessionId`, `label`, and `isSupervisor` so the fleet's shape is visible. This bookkeeping lives in memory — it does not survive an app restart.",
          '**Structured results.** A dispatch can ask a worker for a JSON-Schema-shaped answer; the worker returns it as a fenced `wks-result` block, which is validated and rendered as a real card — on desktop and on the `/m` phone client — instead of a raw JSON dump in the transcript.',
          '**Mobile fleet-wake detection.** `/m` now distinguishes a worker that\'s merely taking a while from one that\'s actually stuck: a working agent with no progress for several minutes raises a "Not moving" item, and one whose status line has gone silent raises "No signal" instead.',
          '**Tiered dispatch tokens.** Workers can be minted a capability token scoped to view, triage, or operator tools, derived from one allowlist shared by the MCP facade and the hub — so a read-only scout genuinely cannot mutate anything.',
          "**Worktree setup hooks + node_modules auto-link.** Project-defined hooks run deterministically when a worktree is created for a dispatched agent, and `node_modules` is auto-linked into it at whatever depth the project needs, so a fresh worktree doesn't need a full reinstall to run.",
          "**Hub federation, on every client.** Two machines can be one fleet: hubs pair with each other (`peers.json`), events are stamped with the hub they came from, and a capability on a peer is called through a `hub:<peer>/` address. Remote sessions are chattable, not just visible, and the desktop, the `/app` browser client, the `/m` phone client and `wks-tui` are all federation-aware — a peer's agents appear in the fleet with a hub marker, and a session that goes away leaves a tombstone rather than a ghost row. *(Shipped in this release but omitted from its notes at the time.)*",
          '**The hub runs errands: jobs.** Recurring or one-off jobs owned by the hub — spawn an agent, call a bus capability, or run a shell command — with a Settings ▸ Jobs surface, a `workspacer jobs` CLI, and job-creation RPCs restricted to a trusted caller so an agent may only *suggest* one. A job can run code before it wakes a model, so the cheap check happens first. *(Shipped in this release but omitted from its notes at the time.)*',
        ],
      },
      {
        title: 'Fixed',
        items: [
          '**Security: a federated call could forge caller identity.** `router.call` and the cross-hub `federatedCall` path each maintained their own hand-written list of which fields to strip or stamp for caller identity before dispatch (profile/yolo-grant on `agents.spawn`, `callerSessionId` on `agents.reportProgress`). The two lists could drift, and one gap did: a view-tier token could forge a `callerSessionId` across a federated hop. Both paths now consult a single sanitizer table so they cannot diverge again.',
          '**Hardened the pending-message-slot invariant** across the Rust session state machine, the hook feed, and the store, closing several races where a send could be dropped or a parked approval clobbered.',
          'Numerous smaller fixes to worker-finished wake reliability, model/profile respawn duplicate cards, and sidebar/grandchild session display.',
        ],
      },
      {
        title: 'Changed',
        items: [
          '`CLAUDE.md` and the `.rivet/context` documentation are now tracked in the repository.',
          'The mobile end-to-end suite (`/m`, Playwright) was repaired — it had been silently unrun for two days — and is now wired into CI on every PR.',
        ],
      },
    ],
  },
  {
    version: '0.149.0',
    date: '2026-08-14',
    unreleased: false,
    sections: [
      {
        title: 'Added',
        items: [
          "**Widgets.** A project's widget board lives in the inspector rail's Project tab: small (1x1), medium (2x1) and large (2x2) tiles pinned to a *directory* rather than to a session, so every agent working in that repo sees the same board. Built-in git and usage tiles ship with it, and plugins can contribute their own — a widget is declared separately from a pane, because a pane may own a PTY or a 1.2MB editor bundle and none of that reads at 150px.",
          '**Release notes in the app.** Settings → Updates lists every release, newest first, with the running version marked. A one-time notice appears the first time an install runs a new version. Both read the same `CHANGELOG.md` the GitHub release page is cut from, so they cannot disagree.',
          '**See what\'s new before you restart.** The "Update ready" prompt has a third button that opens the release notes for the build it is offering. It then asks again rather than treating "I read them" as an answer. The notes open on the release page because a running build cannot contain them — its copy of the changelog was baked before that release existed.',
          '**Notifications say what the agent actually said.** They used to name the event and nothing else — "Approve a tool use" told you an agent was blocked but not on what. They now carry the tool and its argument (`Bash rm -rf build/`), the text of the question being asked, or the tail of the reply that just finished. It is per device and switchable, because that text lands on a lock screen: **Show message contents**, on by default. There is also an opt-in **Ping while still running**, at 10 and 30 minutes into a single run, for when you have walked away from something long.',
          '**"Send me a test notification", and settings you can actually reach.** The phone can now provoke a push on demand and reports how many devices it went to, so "I got nothing" stops being a scavenger hunt across trigger, delivery, subscription and permission — it deliberately ignores the per-kind switches, since a test something could silence cannot tell muted from broken. The More sheet also opens without an agent selected (it used to be a dead button on the screen the app lands on), and the notification banner now explains itself when push is unavailable — "Add to Home Screen" or "needs an HTTPS address" — instead of rendering nothing at all, which is what made this invisible.',
          '**More kinds of phone notification, and per-device control of them.** The phone used to buzz for one thing: an agent blocked on you. It now also notifies when a run *finishes* and when a session *ends* — and because every turn ends, "finished" only fires for runs longer than a threshold, so the twenty-second exchange you are watching stays silent while the thing you walked away from tells you it is done. Each kind is switchable per device (More → Notification settings), with the length threshold picked from Any / 1m / 5m / 15m, so a phone and a tablet need not agree.',
          '**Spawn an agent from the command palette.** It already could, under a name nobody would search for.',
          "**Projects have a face.** A fleet of a dozen agent cards had exactly one thing saying which repo each belonged to: its cwd, in a tooltip. Every directory now carries a mark — initials from its name, a colour from its path — shown on the sidebar cards, the Fleet Deck, the spawn dialog, the overview, and the TUI's sidebar and roster. The point is that it works with **no configuration**: the fleet is legible the first time you look at it, and Settings → Projects is where you override something you didn't like rather than a form you must fill in before anything happens. Initials break on word boundaries, so `api-gateway` and `api-worker` don't both read `AP`. Override with an emoji, a favicon, a label or a colour; a project you have *named* names its agents too, instead of every one of them being called after the folder it happened to start in.",
          '**Per-project plugin settings.** A plugin marks a setting `"scope": "project"` and it is stored against the directory and edited on the Projects page beside that project\'s identity. Three plugins had already invented this privately — shiplight\'s repo, ci-watcher\'s repo, jira\'s directory-to-prefix map — each with its own storage and its own editor. A plugin reads the values through the `config.get` it can already declare, so this needed no new capability. Project scope names *which* thing; the plugin\'s own settings file still holds the key to it, and "project-scoped **and** secret" is refused when the plugin loads.',
          '**Forget a project.** A project entry outlived its directory: a repo you deleted kept its row, its icon and its plugin settings forever. The Projects page now marks the ones whose directory is gone and offers to forget them.',
          '**Claude\'s built-in skills are visible.** The Library pane showed zero skills in a repo with no `.claude/` of its own, and the Context pane showed eighteen names with nothing behind sixteen of them — both only ever looked in `<cwd>/.claude`. All the roots are read now (project, then user, then plugins), including `CLAUDE_CONFIG_DIR` for the first time, which is what Claude profiles set. Of eighteen skills a session reports, three are files; the other fifteen are compiled into the `claude` binary, so those now say **built-in** rather than leaving a blank row. A `Skill` tool call also gets a proper card — it was falling through to "the first string value in the input", so any invocation carrying arguments printed your argument text where the skill\'s name belongs.',
        ],
      },
      {
        title: 'Changed',
        items: [
          "**A widget tile has no title bar.** The host used to draw the widget's name above it, which spent a fifth of a 148px square restating what the tile already said and pushed the content into a corner. Tiles now own their whole square and small ones centre; the name moved to the picker and to Edit mode, which is when you are identifying a tile rather than reading it.",
          "**The editor's file tree reads like the rest of the app.** It picked up the review pane's tree language: inset rounded rows, a tinted accent selection instead of a solid fill, and single-child folder chains collapsed into one row (`apps/desktop/src` rather than three). The sidebar is on the app's sans now — it is chrome, not code — while the file bar and status bar stay mono.",
          '**The plugin docs now say `agent.snapshot` carries a conversation *window*.** It always described a "full per-agent snapshot"; nothing told an author the transcript on it is bounded, so the first plugin to read `data.conversation` would have got twelve turns with no way to know that was by design. The note points at `sessions.conversation` (`sinceSeq`) for anyone who needs real history, and at the wake-up-and-re-query pattern for anyone who does not.',
          '**Shiplight\'s pipeline widget shows a stopwatch, not an age.** A running pipeline gets a live `m:ss` clock and a finished one shows how long it took — "4m" looks identical whether a run is alive or wedged.',
          "**A pasted project icon is downloaded once, not hot-linked forever.** The URL used to go straight into an `<img src>`, so the mark vanished offline, the request repeated on every render, and the obvious way to get such a URL — a third-party favicon service — was told every project domain you work on. It is fetched once now and served from disk. `http(s)` only, 2 MB cap, 8s timeout, and it has to actually be an image; the file extension comes from the response's content type and never from the URL.",
          '**Your recent projects are a timestamp, not a capped list.** Opening a project used to shuffle a fixed-length array, so a project you had configured could fall off it because you opened eight others. Pins and ordering from an older config are read as they were, and never rewritten — downgrading loses nothing.',
        ],
      },
      {
        title: 'Fixed',
        items: [
          '**Web Push never worked on iPhone at all.** Every notification to `web.push.apple.com` came back `403` and always had: the VAPID `sub` claim was `mailto:workspacer@localhost`, and Apple requires a real domain or an `https:` URL. Chrome and Android accept it, so the one push service that refused it was the one the feature was built for — and the send path discarded its error without a log, so the refusal was invisible from the moment it shipped. Send failures are now logged with the push service and status code, and the phone\'s test button reports what was actually delivered rather than how many subscriptions it tried (it said "sent to 4 devices" while four of them were being refused). Existing subscriptions keep working; the subject is a claim inside the signed JWT, not part of the keypair.',
          '**The phone client could sit on "Offline" far longer than it needed to.** Its handshake watchdog gave every connection attempt a flat 4.5 seconds — but the case it exists for is a sleeping host or a cold tailnet link, which takes far longer than that. So it killed the very attempt it was written to survive, over and over, and only recovered once repeated tries had warmed the route. The budget now grows with the retry backoff (4.5s → 9s → 20s), and tapping the status pill while it reads "Offline" retries immediately instead of waiting out the backoff.',
          '**Your newest message stopped riding the top of the viewport.** Sending scrolled the transcript so your message sat at the top with the reply growing into the space below it; since 0.148.0 the message was landing at the bottom instead, with the blank space stranded below the fold. The spacer was being built correctly and never scrolled into: the scroll the pin itself issued was being read as *you* scrolling away, which unstuck the pin a few milliseconds after it armed.',
          "**Connecting a web or remote client took far longer than it should have.** The hub published every session's entire transcript on every flush of every session — for a 500-turn agent that is 2.73 MB per push, so a fleet of fifteen cost 41 MB before the window rendered anything and a single streaming agent pushed ~27 MB/s at every connected client. Snapshots now carry a bounded window of the newest turns (2.73 MB → 160 KB, 94% smaller; connect 41 MB → 2.3 MB), anchored so a client holding full history splices it in rather than losing scrollback. This applied to the desktop too, not just browsers, any time remote sharing was on. Plugins that read `agent.snapshot` should treat the conversation as a window — see the note in the plugin docs.",
          "**The editor's file tree did not follow the file you opened.** Opening a file from outside the tree — right-click → Open in editor, a search hit, or a file you just created — loaded it into the editor while the sidebar stayed wherever it was, with nothing selected. The tree now expands to the file, highlights it and scrolls it into view, and keeps that highlight across a Refresh or an on-disk change. The plugin also read paths as POSIX-only, so on Windows every file was titled with its whole path instead of its name.",
          '**Settings that would not stick.** With remote sharing on, the hub handed the brain its own wildcard bind address as a *dial* URL, which the DNS-rebinding guard correctly refused — so the brain reconnected into a 403 forever with its output going to `/dev/null`, and every capability it is the sole provider for (config, library, saved layouts, sessions, profiles) answered "no provider" on the bus. The app runs on that bus by default, so every settings write failed silently. It reached people as a widget board that would not persist.',
          "**HTTPS via Tailscale returned 403 on every route.** The Remote Share dialog's one-tap toggle fronts a loopback hub with `tailscale serve`, which forwards a public `Host` to a loopback socket — indistinguishable from the rebinding shape the hub refuses. It was the only path to the secure context the phone client and Web Push need.",
          '**Writes that reported success without reaching disk.** The desktop config writer adopted and then served a value it had failed to write; the hub\'s layout document reported a version bump for a document that never landed; the quit-save handshake said "saved" on failure; the web Settings pane printed a green "Saved" for a pricing write that was a no-op stub. The TUI said "Renamed" into a directory it never created, so on a TUI-only machine every rename, pin and note had been failing silently since the feature shipped.',
          '**The live-agent view could go permanently empty under `workspacer serve`.** A non-2xx from the daemon was read as a clean end-of-stream and both callers discarded even that, so a permanent failure produced no log line anywhere.',
          '**A same-millisecond write by the other process was invisible to both config writers**, and was silently reverted through the lock with both sides reporting success.',
          "**Setting an icon on one project wiped every other project's identity.** Three config maps are replaced outright on save rather than merged, because a merge can only add keys — under one, an entry you just deleted would come straight back. The renderer trims every save down to what actually changed, so it has to leave those maps whole; both ends listed them and the lists drifted. The result was that saving one project shipped a one-entry map that the writer took as the whole truth, so every other project lost its icon, label, colour, pin and per-project plugin settings and fell back to derived initials. There is one list now, imported by both ends.",
          '**`push.test` reached the consent dialog as a raw method id.** The capability that provokes a test notification had no label, so the prompt asked you to approve `push.test` instead of "Send a test push notification" — the guard that catches exactly this had been failing, and was dismissed as pre-existing.',
        ],
      },
      {
        title: 'Security',
        items: [
          "**The library's MCP editor wrote API tokens in the clear.** It invites you to type a token into a server's `env` or `headers`, then wrote it as plain text into markdown under `.workspacer/library` — which the service's own header describes as per-repo and committable. No attacker was needed; `git add -A` was enough. Both fields are now redacted on the way out, and an echoed placeholder resolves back against what is stored so a round-trip through the masked editor keeps the real token. Redaction is the default and the spawn paths opt in by name: a missed redaction leaks a live credential, while a missed *un*-redaction produces a visibly broken token and a bug report.",
          '**Path containment on Windows.** Two escapes, neither reachable on Linux and both found the first time the guards were executed on Windows at all. A path *through a regular file* was allowed, because Windows reports that as not-exist where POSIX says ENOTDIR. And a trailing space made `..` a literal child name to the guard while Win32 read it as a parent traversal — enough to list the config directory, which holds the token that promotes a bus connection to trusted. All three copies of the containment rule carried both.',
          '**The git per-user-config gate was off on Windows**, so a dotfiles-symlinked `~/.gitconfig` was an ordinary writable file — and `filter.<drv>.clean` is command execution one write away.',
          '**Seven rounds of adversarial hardening** across the capability plane: plugin grants, boot-document restore, the event and HTTP planes, provider adapters, session lifecycle, and the twins that implement each rule more than once. The method and the findings are written up in `docs/adversarial-hardening.md`.',
        ],
      },
    ],
  },
  {
    version: '0.148.0',
    date: '2026-07-29',
    unreleased: false,
    sections: [
      {
        title: 'Added',
        items: [
          "**Resizable sidebar.** Drag the panel's right edge to set any width from 220px to 560px (double-click to reset; arrow keys nudge once the handle has focus). The width persists in `config.ui.sidebarWidth` and is re-clamped to at most 45% of the window on load, so a width set on an ultrawide can't swallow a laptop screen. A drag writes config once on release, not once per frame.",
          '**The chat says how long it has been working.** A live elapsed timer on the in-progress turn, driven by the turn actually being open (an approval-parked turn is not over) rather than by the streaming flag.',
          "**Copy button on agent messages.** Hover an assistant message for a quiet action row that copies its raw markdown; the row reserves its height whether revealed or not, so it can't reflow a streaming transcript.",
          '**Sent messages admit they aren\'t acknowledged yet.** Until the daemon echoes the turn back, the bubble is dimmed and marked "Sending…" or "Queued".',
        ],
      },
      {
        title: 'Changed',
        items: [
          '**The sidebar\'s bottom belongs to the feed.** The full-width "Spawn agent" pill and the hub status row are gone. Spawn is now an icon in the header cluster beside the notification bell, and the mobile-client (Remote control) phone icon moved there with it — the same three actions, in the same order, in the collapsed rail. The History row pins to the real bottom of the panel.',
          'Your newest message rides the top of the viewport while the reply streams in below it, instead of both being crushed against the composer.',
          'Stop moved into the composer beside send rather than replacing it, so you can still queue a message mid-turn. Esc still cancels.',
          'The chat column is narrower (900px) and now a token, `--wks-chat-width`.',
        ],
      },
      {
        title: 'Removed',
        items: [
          'The hub connection dot. Reconnect was always automatic (`useHubReconnect`, and plugins re-subscribe) — the indicator was something to watch, not to act on. The "? Help" button in that row went with it; help is still F1, the command palette, and onboarding.',
        ],
      },
    ],
  },
  {
    version: '0.144.0',
    date: '2026-07-22',
    unreleased: false,
    sections: [
      {
        title: 'Added',
        items: [
          "**Real plugin update detection.** Plugin manifests now carry a `version` field, and the Plugins Manager checks each installed plugin's install source for a newer published version instead of showing a permanent Reinstall button. A new guarded hub route `POST /plugins/updates` re-fetches every sourced plugin's manifest and compares versions; the pane shows a version chip, a **Check for updates** action, an accent **Update → vX** button only when the source is genuinely newer, and a muted **Reinstall** otherwise.",
          "The **Browse catalog** dialog now shows each plugin's published version.",
        ],
      },
      {
        title: 'Changed',
        items: [
          'Every plugin in the public catalog (and the bundled example plugins) is baselined at `1.0.0`; `build_index.py` now carries `version` into `index.json`.',
          'Documented the `version` field and the version-driven update flow in the build-a-plugin landing docs.',
        ],
      },
      {
        title: 'Fixed',
        items: [
          'The Plugins Manager no longer implies an update is available for every plugin installed from a source. A plugin with no declared `version` reports no update (it can still be reinstalled on demand), so the badge only appears for a real upgrade.',
        ],
      },
    ],
  },
  {
    version: '0.143.0',
    date: '2026-07-21',
    unreleased: false,
    sections: [
      {
        title: 'Added',
        items: ['Review pane **history mode**: browse recent commits and their diffs.'],
      },
      {
        title: 'Changed',
        items: [
          'Plugin panes split in place, scoped to the active agent; opening a global pane from an agent workspace highlights the right tab.',
          'Sidebar EARLIER and RECENT sections dock to the bottom of the feed.',
        ],
      },
      {
        title: 'Fixed',
        items: [
          'Plugin failures stay contained to their pane (focus recovery, timeouts, a Ctrl+P fallback), and a plugin update can no longer gut a live install on Windows file locks.',
          'The command palette can never trap the keyboard; webview key forwarding derives from the live keybinding config.',
          'Conversation no longer flickers when switching agents in stream mode.',
        ],
      },
    ],
  },
  {
    version: '0.142.0',
    date: '2026-07-20',
    unreleased: false,
    sections: [
      {
        title: 'Added',
        items: ['**Keep-warm** learns Codex: per-provider heartbeats warm ChatGPT windows too.'],
      },
      {
        title: 'Fixed',
        items: [
          'Agents reconnect after a reboot (reconcile + auto-resume at boot); terminated agents reach the sidebar RECENT list promptly.',
        ],
      },
    ],
  },
  {
    version: '0.141.0',
    date: '2026-07-20',
    unreleased: false,
    sections: [
      {
        title: 'Added',
        items: [
          '**Keep-warm**: auto-start the 5h rate-limit window on a schedule; keep-warm pings become logged, listable claudemon heartbeats.',
          'PostHog analytics on the landing pages.',
        ],
      },
      {
        title: 'Changed',
        items: [
          'Styling consistency sweep: icons over symbols, single-source design tokens.',
          'Stop interrupting agents on usage warnings; keep the accurate gauges.',
        ],
      },
    ],
  },
  {
    version: '0.140.0',
    date: '2026-07-19',
    unreleased: false,
    sections: [
      {
        title: 'Added',
        items: [
          'Approval cards for stream-transport Claude sessions, resolved structurally from the inbox.',
        ],
      },
      {
        title: 'Changed',
        items: ['The needs-you dock is minimizable and denser.'],
      },
    ],
  },
  {
    version: '0.139.0',
    date: '2026-07-19',
    unreleased: false,
    sections: [
      {
        title: 'Changed',
        items: [
          'Expanded sidebar reworked into live activity-feed cards (action log, provider hues, inline Approve/Reply); RECENT lists resumable sessions with auto titles.',
          'Single implicit session — named sessions removed; boot restores the most recent. Stopped daemon rows revive on resume; attach viewers stay alive.',
        ],
      },
    ],
  },
  {
    version: '0.138.0',
    date: '2026-07-18',
    unreleased: false,
    sections: [
      {
        title: 'Added',
        items: [
          'Plugin authoring loop: `workspacer plugin dev` hot-reload, sidecar log streaming, and a `window.workspacer` SDK auto-injected into plugin webviews.',
          '`make-workspacer-plugin` skill + a build-a-plugin landing page.',
        ],
      },
      {
        title: 'Changed',
        items: [
          'New configs default to the everforest theme; a single `DEFAULT_THEME` constant replaces scattered `dark` fallbacks.',
        ],
      },
    ],
  },
];
