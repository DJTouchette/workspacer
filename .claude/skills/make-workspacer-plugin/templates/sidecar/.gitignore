# Loader-owned files — the hub writes these into the plugin dir. Never commit.
.bus-token
.settings.json
.install-source
.disabled

# Node deps (restored by the `install` command in plugin.json)
node_modules/

# Misc
*.log
.DS_Store
