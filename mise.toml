[tools]
# Go: hub/ requires go 1.25.4 (see hub/go.mod)
go = "1.25"
# Node: Electron app + renderer (README requires Node 18+; pin to current LTS)
node = "22"

[env]
# Ensure project-local node_modules binaries resolve in tasks
_.path = ["./node_modules/.bin"]
