/**
 * Single source of truth for the claudemon daemon base URL.
 * All claudemon API clients import this constant instead of duplicating it.
 */
export const CLAUDEMON_API_BASE = 'http://127.0.0.1:7891';
